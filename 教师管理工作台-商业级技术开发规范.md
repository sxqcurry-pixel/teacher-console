# 教师管理工作台 — 商业级技术开发规范

> 目标：以成熟软件公司的工程标准，交付一套可维护、可扩展、低耦合的教师教学管理系统。本文档作为 Code 模式开发的总纲，包含架构、目录、数据库、API、模块设计与分阶段实施计划。

---

## 1. 技术选型与理由

### 1.1 前端

| 层 | 技术 | 选型理由 |
|----|------|---------|
| 框架 | **Next.js 14+ (App Router)** | React 生态最成熟的 SSR/SSG 框架，支持 RSC、Server Actions，利于 SEO 与首屏性能 |
| 语言 | **TypeScript 5.x** | 类型安全、重构友好、团队协作必备 |
| 样式 | **Tailwind CSS + CSS Variables** | 原子化 + 设计令牌，便于维护深色主题与品牌一致性 |
| 组件库 | **shadcn/ui + Radix UI** | 商业级可复用组件，源码在项目中，可深度定制 |
| 状态 | **Zustand + TanStack Query** | Zustand 轻量、无样板；React Query 管理服务端状态、缓存、同步 |
| 动效 | **GSAP + Framer Motion** | GSAP 负责复杂运镜与转盘物理动画，Framer Motion 负责组件级微交互 |
| 图表 | **Recharts / Tremor** | React 原生图表，易于封装为业务组件 |
| 表格 | **TanStack Table v8** | 高性能、可扩展的数据表格 |
| 表单 | **React Hook Form + Zod** | 类型安全表单与校验 |
| PWA | **next-pwa / serwist** | 支持离线缓存与桌面/移动端安装 |

### 1.2 后端

| 层 | 技术 | 选型理由 |
|----|------|---------|
| 框架 | **NestJS 10+** | 企业级 Node.js 框架，内置 DI、模块化、Guard/Interceptor/Pipe，适合复杂业务 |
| 语言 | **TypeScript** | 前后端统一语言，类型共享 |
| ORM | **Prisma** | 类型安全、迁移管理、查询优化 |
| 数据库 | **PostgreSQL 15+** | 关系型主库，支持 JSONB、窗口函数、复杂查询 |
| 缓存 | **Redis** | 会话、实时锁、排行榜、消息队列 |
| 实时 | **Socket.IO / ws** | 双端同步、课堂转盘状态广播 |
| 认证 | **Clerk / Auth.js (NextAuth v5)** | 支持账号密码、微信扫码、OAuth |
| 文件 | **MinIO (S3 协议)** | 本地/私有云对象存储，学生批量导入文件、头像 |
| 队列 | **BullMQ (Redis)** | 异步任务：Excel 导入解析、AI 报告生成 |
| 日志 | **Pino + OpenTelemetry** | 结构化日志与可观测性 |

### 1.3 AI 与外部服务

| 能力 | 技术 |
|------|------|
| 大模型 | 豆包 / 通义千问 / OpenAI（通过统一 Provider 接口封装） |
| Embedding | 文本向量库（pgvector）用于备课素材语义检索 |
| Excel 解析 | 前端 SheetJS 预览 + 后端 Python pandas/openpyxl 精确解析 |

---

## 2. 系统架构（Clean Architecture + 分层）

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Layer                              │
│  PC Web (Next.js)    Mobile Web (Next.js Responsive/PWA)         │
└───────────────────────┬─────────────────────────────────────────┘
                        │ HTTPS / WebSocket
┌───────────────────────▼─────────────────────────────────────────┐
│                      Gateway Layer                               │
│  Nginx / Vercel Edge  →  Auth / Rate Limit / CORS               │
└───────────────────────┬─────────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                   Application Layer (NestJS)                     │
│  Controllers → DTOs → Services → Use Cases → Domain Services     │
│  Guards / Interceptors / Pipes / Filters / Modules               │
└───────────────────────┬─────────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                    Domain Layer (Pure TS)                        │
│  Entities / Value Objects / Repository Interfaces / Domain Events│
└───────────────────────┬─────────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                  Infrastructure Layer                            │
│  Prisma Repositories / Redis / Socket.IO / S3 / AI Provider      │
└─────────────────────────────────────────────────────────────────┘
```

**核心原则**
- **依赖向内**：外层依赖内层，Domain 层不依赖任何框架。
- **接口隔离**：Repository、AI Provider、FileStorage 全部面向接口编程。
- **模块化**：每个业务模块独立（Student、Score、Point、Wheel、AI、Communication）。
- **事件驱动**：关键状态变化发布 Domain Event，异步处理副作用（同步、通知、统计）。

---

## 3. 项目目录结构

```
teacher-workspace/
├── apps/
│   ├── web/                          # Next.js 前端
│   │   ├── src/
│   │   │   ├── app/                  # App Router
│   │   │   │   ├── (dashboard)/      # 登录后布局
│   │   │   │   │   ├── dashboard/
│   │   │   │   │   ├── students/
│   │   │   │   │   ├── scores/
│   │   │   │   │   ├── points/
│   │   │   │   │   ├── wheel/
│   │   │   │   │   ├── ai/
│   │   │   │   │   └── todos/
│   │   │   │   ├── auth/
│   │   │   │   └── layout.tsx
│   │   │   ├── components/
│   │   │   │   ├── ui/               # shadcn 基础组件
│   │   │   │   ├── layout/           # Sidebar / Header / BottomNav
│   │   │   │   ├── data-display/     # Cards / Tables / Charts
│   │   │   │   ├── feedback/         # Toast / Modal / Loading
│   │   │   │   └── animation/        # GSAP / Motion 包装组件
│   │   │   ├── hooks/                # 自定义 Hooks
│   │   │   ├── stores/               # Zustand stores
│   │   │   ├── lib/
│   │   │   │   ├── api/              # API client (axios/fetch)
│   │   │   │   ├── utils/
│   │   │   │   └── constants/
│   │   │   ├── styles/
│   │   │   └── types/                # 共享 DTO 类型
│   │   ├── public/
│   │   └── next.config.js
│   │
│   └── api/                          # NestJS 后端
│       ├── src/
│       │   ├── main.ts
│       │   ├── app.module.ts
│       │   ├── config/               # 配置模块
│       │   ├── common/               # 通用：拦截器、过滤器、装饰器、 pipes
│       │   ├── modules/
│       │   │   ├── auth/
│       │   │   ├── user/
│       │   │   ├── student/
│       │   │   ├── score/
│       │   │   ├── point/
│       │   │   ├── wheel/
│       │   │   ├── communication/
│       │   │   ├── todo/
│       │   │   ├── material/
│       │   │   ├── ai/
│       │   │   └── sync/
│       │   ├── domain/
│       │   │   ├── entities/
│       │   │   ├── value-objects/
│       │   │   ├── repositories/
│       │   │   └── events/
│       │   ├── infrastructure/
│       │   │   ├── prisma/
│       │   │   ├── redis/
│       │   │   ├── storage/
│       │   │   ├── ai/
│       │   │   └── websocket/
│       │   └── shared/
│       ├── prisma/
│       │   ├── schema.prisma
│       │   └── migrations/
│       └── test/
│
├── packages/
│   ├── shared/                       # 前后端共享类型与工具
│   │   ├── src/
│   │   │   ├── dto/
│   │   │   ├── enums/
│   │   │   ├── types/
│   │   │   └── utils/
│   │   └── package.json
│   └── ui/                           # 跨项目 UI 组件（可选）
│
├── docker-compose.yml                # PG + Redis + MinIO
├── turbo.json                        # Monorepo 任务编排
└── pnpm-workspace.yaml
```

---

## 4. 数据库设计（PostgreSQL + Prisma）

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String
  avatar    String?
  role      Role     @default(TEACHER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  classes   Class[]
  todos     Todo[]
}

model Class {
  id        String   @id @default(cuid())
  name      String
  grade     String
  subject   String
  teacherId String
  teacher   User     @relation(fields: [teacherId], references: [id])
  students  Student[]
  lessons   Lesson[]
}

model Student {
  id        String   @id @default(cuid())
  serialNo  Int
  name      String
  remark    String?
  status    StudentStatus @default(ACTIVE)
  classId   String
  class     Class    @relation(fields: [classId], references: [id], onDelete: Cascade)

  scores    Score[]
  points    Point[]
  communications Communication[]

  @@unique([classId, serialNo])
}

model Lesson {
  id        String   @id @default(cuid())
  index     Int
  title     String
  fullScore Int      @default(30)
  classId   String
  class     Class    @relation(fields: [classId], references: [id])
  scores    Score[]
  points    Point[]
}

model Score {
  id        String   @id @default(cuid())
  studentId String
  student   Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)
  lessonId  String?
  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
  type      ScoreType
  rawScore  Float?
  weightedScore Float?
  rank      Int?
  result    String?
  remark    String?
  createdAt DateTime @default(now())

  @@unique([studentId, lessonId, type])
}

model Point {
  id        String   @id @default(cuid())
  studentId String
  student   Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)
  lessonId  String?
  lesson    Lesson?  @relation(fields: [lessonId], references: [id])
  category  PointCategory
  score     Int
  reason    String?
  createdAt DateTime @default(now())
}

model Auction {
  id          String   @id @default(cuid())
  title       String
  description String?
  startingPrice Int
  currentPrice  Int    @default(0)
  winnerId    String?
  status      AuctionStatus @default(OPEN)
  expiresAt   DateTime
  createdAt   DateTime @default(now())
}

model Communication {
  id            String   @id @default(cuid())
  studentId     String
  student       Student  @relation(fields: [studentId], references: [id], onDelete: Cascade)
  type          CommType
  content       String
  followUp      String?
  renewalStatus RenewalStatus?
  createdAt     DateTime @default(now())
}

model Todo {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  title     String
  completed Boolean  @default(false)
  dueDate   DateTime?
  category  TodoCategory
}

model Material {
  id        String   @id @default(cuid())
  userId    String
  title     String
  content   String
  tags      String[]
  embedding Unsupported("vector")?
  createdAt DateTime @default(now())
}

enum Role { TEACHER ADMIN }
enum StudentStatus { ACTIVE INACTIVE TRIAL }
enum ScoreType { LESSON STAGE_TEST }
enum PointCategory { ANSWER TOP3 HOMEWORK_ON_TIME HOMEWORK_QUALITY NOTE_QUALITY OTHER }
enum AuctionStatus { OPEN CLOSED }
enum CommType { PHONE WECHAT FACE_TO_FACE }
enum RenewalStatus { HIGH MEDIUM LOW }
enum TodoCategory { LESSON_PREP FOLLOW_UP RENEWAL ADMIN }
```

---

## 5. API 设计规范（REST + WebSocket）

### 5.1 REST 规范

- 基础路径：`/api/v1/{module}`
- 统一响应：
  ```ts
  interface ApiResponse<T> {
    code: number;
    message: string;
    data: T;
    timestamp: string;
  }
  ```
- 分页请求：
  ```ts
  interface PageQuery {
    page: number;
    pageSize: number;
    sortBy?: string;
    sortOrder?: 'asc' | 'desc';
    keyword?: string;
  }
  ```

### 5.2 核心接口示例

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| Auth | POST | `/api/v1/auth/login` | 登录 |
| Auth | POST | `/api/v1/auth/register` | 注册 |
| Student | GET | `/api/v1/students` | 学生列表 |
| Student | POST | `/api/v1/students/bulk-import` | Excel 批量导入 |
| Student | GET | `/api/v1/students/export` | 导出 Excel |
| Score | GET | `/api/v1/scores?classId=&lessonIndex=` | 成绩列表 |
| Score | POST | `/api/v1/scores/batch` | 批量录入成绩 |
| Point | GET | `/api/v1/points` | 积分明细 |
| Point | POST | `/api/v1/points` | 增减积分 |
| Wheel | POST | `/api/v1/wheel/spin` | 转盘抽奖 |
| Wheel | GET | `/api/v1/wheel/history` | 历史记录 |
| AI | POST | `/api/v1/ai/advice` | 获取学习/教学建议 |
| Sync | WS | `/ws/sync` | 实时同步通道 |

### 5.3 WebSocket 协议

```json
// 客户端订阅
{ "type": "SUBSCRIBE", "channel": "class:{classId}" }

// 服务端广播（成绩更新示例）
{
  "type": "DATA_CHANGED",
  "channel": "class:{classId}",
  "payload": {
    "entity": "SCORE",
    "action": "UPDATED",
    "data": { "studentId": "...", "lessonId": "...", "score": 28 }
  }
}
```

---

## 6. 前端架构

### 6.1 状态分层

```
Server State  → TanStack Query（缓存、同步、失效）
Global UI     → Zustand（sidebar、theme、toast、modal）
Local Form    → React Hook Form（组件级，不上升）
Real-time     → WebSocket → QueryClient.invalidateQueries()
```

### 6.2 关键 Store 示例

```ts
// stores/app-store.ts
import { create } from 'zustand';

interface AppState {
  sidebarOpen: boolean;
  activeClassId: string | null;
  setSidebarOpen: (open: boolean) => void;
  setActiveClassId: (id: string) => void;
}

export const useAppStore = create<AppState>((set) => ({
  sidebarOpen: true,
  activeClassId: null,
  setSidebarOpen: (open) => set({ sidebarOpen: open }),
  setActiveClassId: (id) => set({ activeClassId: id }),
}));
```

### 6.3 API Client（类型安全）

```ts
// lib/api/client.ts
import axios from 'axios';
import type { ApiResponse } from '@shared/dto';

const api = axios.create({ baseURL: process.env.NEXT_PUBLIC_API_URL });

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

export async function getStudents(params: StudentQuery) {
  const { data } = await api.get<ApiResponse<Student[]>>('/api/v1/students', { params });
  return data.data;
}
```

### 6.4 动画策略

| 场景 | 库 | 实现 |
|------|----|------|
| 页面切换 | GSAP | `pageTransition` 包装，模拟镜头推进/平移 |
| 列表/卡片入场 | Framer Motion | `staggerChildren` + `variants` |
| 数字滚动 | GSAP | `gsap.to(target, { innerText, snap })` |
| 转盘 | GSAP + Canvas/SVG | 惯性旋转 + 缓动停止 |
| 按钮/悬浮 | Framer Motion | `whileHover`, `whileTap` |
| 背景光效 | CSS / Three.js | 微粒子或渐变网格（轻量） |

---

## 7. 核心模块设计

### 7.1 学生模块（Student）

**Use Cases**
- `CreateStudentUseCase`
- `UpdateStudentUseCase`
- `BulkImportStudentsUseCase`
- `ExportStudentsUseCase`
- `QueryStudentsUseCase`

**文件上传流程**
1. 前端拖拽上传 `.xlsx`
2. 后端接收后存入 MinIO
3. BullMQ 队列异步解析
4. Python 服务或 Node.js `xlsx` 读取数据
5. 校验后写入 `Student` 表
6. 通过 WebSocket 通知前端导入完成

### 7.2 成绩模块（Score）

**核心逻辑**
- 讲次成绩满分 30
- 阶段测满分 100，折算为 `rawScore * 0.3`
- 加权总分 = 讲次平均分 + 阶段测折算分
- 排名使用 PostgreSQL 窗口函数 `RANK()` 计算

**批量更新接口**
```ts
POST /api/v1/scores/batch
{
  "lessonId": "...",
  "scores": [
    { "studentId": "...", "rawScore": 28, "remark": "" }
  ]
}
```

### 7.3 积分模块（Point）

**领域规则**
- 积分类别与 Excel 结构对齐
- 总分实时聚合
- 支持撤销（软删除或反向记录）

**竞拍模块**
- `Auction` 实体管理拍品
- 出价记录 `AuctionBid` 表
- Redis 分布式锁防止并发超卖
- 倒计时使用 Redis TTL 或 schedule

### 7.4 转盘模块（Wheel）

**算法**
- 权重随机：使用 `crypto.getRandomValues` 或服务端加权随机
- 淘汰模式：中奖项从候选池移除
- 历史记录持久化

**实现**
- 前端 SVG/Canvas 绘制转盘
- GSAP 控制旋转动画（初始加速、惯性、减速停止）
- 结果通过 API 校验后落库

### 7.5 AI 工作台

**架构**
- `AIProvider` 接口封装不同大模型
- Prompt Template 管理（学生建议、教学建议、素材检索）
- 选项式交互：前端预置选项 → 拼接 Prompt → 调用 LLM → 流式返回

**Prompt 示例**
```
你是一位资深初中数学教师。请根据以下学生数据给出学习建议：
- 姓名：{name}
- 最近三次成绩：{scores}
- 积分情况：{points}
- 薄弱点：{weakTags}

请给出 3 条具体、可执行的学习建议。
```

### 7.6 实时同步模块（Sync）

**策略：事件广播 + 乐观更新**
1. 用户 A 修改数据
2. 前端乐观更新 UI
3. API 请求成功后发布 Domain Event
4. Sync Service 通过 Redis Pub/Sub 广播到该班级所有在线客户端
5. 其他端收到后刷新 TanStack Query 缓存

---

## 8. 安全与权限

- **JWT + Refresh Token**：Access Token 15 分钟，Refresh Token 7 天
- **RBAC**：Teacher / Admin 角色
- **数据隔离**：教师只能访问自己的班级与学生
- **输入校验**：Zod DTO + Class-Validator
- **SQL 注入防护**：Prisma ORM 参数化查询
- **文件安全**：限制上传类型与大小，病毒扫描（可选 ClamAV）
- **审计日志**：关键操作（成绩修改、删除）记录操作人与时间

---

## 9. 开发步骤（Phase 1 → Phase 4）

### Phase 1：基础设施与框架搭建（Day 1-2）

1. 初始化 monorepo（Turborepo + pnpm workspace）
2. 配置 Docker Compose（PostgreSQL + Redis + MinIO）
3. 搭建 NestJS 后端项目结构
4. 搭建 Next.js 前端项目结构
5. 配置 Prisma schema 与首版 migration
6. 配置 Tailwind + shadcn/ui + 设计令牌
7. 配置 ESLint / Prettier / TypeScript 严格模式
8. 配置 Husky + lint-staged（提交前检查）

### Phase 2：核心后端开发（Day 3-6）

1. 用户认证模块（Auth）
2. 班级与学生 CRUD
3. Excel 批量导入/导出（含异步队列）
4. 成绩录入与加权计算
5. 积分增减与排名
6. 沟通记录与续费跟进
7. 教师待办 CRUD
8. 全局异常处理、日志、Swagger 文档

### Phase 3：核心前端开发（Day 7-12）

1. 登录/注册页面
2. Dashboard 仪表盘（数据卡片、图表）
3. 学生管理页（表格、导入弹窗、导出）
4. 成绩登记页（按讲次 Tab、批量输入）
5. 积分管理页（明细、排行榜、竞拍）
6. 转盘互动页（SVG 转盘、历史记录）
7. AI 工作台页（选项式对话、建议卡片）
8. 家校/待办页
9. 响应式适配与 PWA 配置
10. 页面切换动效与微交互

### Phase 4：实时同步与优化（Day 13-16）

1. WebSocket 接入与频道订阅
2. 多端数据同步机制
3. 离线缓存与冲突处理
4. AI Provider 接入与 Prompt 调优
5. 性能优化（虚拟列表、懒加载、代码分割）
6. 端到端测试（Playwright）
7. 部署脚本（Docker / Vercel + Railway / ECS）

---

## 10. 代码规范

- **命名**：文件 kebab-case，类 PascalCase，函数 camelCase，常量 SCREAMING_SNAKE_CASE
- **模块**：每个 NestJS 模块包含 `module.ts / controller.ts / service.ts / dto.ts / repository.ts`
- **前端组件**：React 函数组件，优先使用 Server Components
- **类型**：禁止 `any`，优先使用 `unknown` + 类型收窄
- **错误处理**：统一 `Result<T, E>` 模式或抛出自定义 `DomainException`
- **注释**：复杂业务逻辑必须写清「为什么」，而非「做什么」
- **测试**：核心 Use Case 必须单元测试；关键页面必须 E2E 测试

---

## 11. 推荐启动命令

```bash
# 1. 克隆/创建项目后安装依赖
pnpm install

# 2. 启动基础设施
docker-compose up -d

# 3. 后端：生成 Prisma 客户端并迁移
pnpm --filter api db:migrate
pnpm --filter api db:seed

# 4. 启动后端开发服务
pnpm --filter api dev

# 5. 启动前端开发服务
pnpm --filter web dev

# 6. 打开 Swagger 文档
open http://localhost:3001/api/docs
```

---

## 12. 附：设计稿与素材

- 设计需求文案：`教师管理工作台-设计需求文案.md`
- 品牌 CSS：`teacher-workspace-desktop/colors_and_type.css`
- 风格方向图：`style-direction-orange-dark.jpg`（已选定）

进入 Code 模式后，可直接以本文档为总纲，按 Phase 1-4 逐步实施。
